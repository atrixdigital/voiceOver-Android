<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maximus.voicemessenger">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminPannel" />
        <activity android:name=".addEmp" />
        <activity android:name=".Register" />
        <activity android:name=".Requests" />
        <activity
            android:name=".Admin_Pannel"
            android:label="@string/title_activity_admin__pannel"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".removerUsers" />
        <activity android:name=".recording" />
        <activity android:name=".Inbox" />
        <activity android:name=".Online_Users" />
        <activity android:name=".Sent" />
        <activity android:name=".senderlist"></activity>
    </application>

</manifest>